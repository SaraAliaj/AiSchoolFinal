# Development WebSocket URL (optional, will use default if not set)
VITE_WS_URL=ws://localhost:8080

# Production WebSocket URL (optional, will use default if not set)
# VITE_WS_URL=wss://aiacademia.tech 